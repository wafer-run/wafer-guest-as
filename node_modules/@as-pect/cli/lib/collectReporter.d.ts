import { IAspectConfig } from "./IAspectConfig.js";
import { IReporter } from "@as-pect/core";
import { OptionValues } from "commander";
/** Collect up the reporter asynchronously because modules could be imported. */
export declare function getReporter(opts: OptionValues, aspectConfig: IAspectConfig): Promise<IReporter>;
export declare function collectReporter(opts: OptionValues, aspectConfig: IAspectConfig): Promise<IReporter>;
//# sourceMappingURL=collectReporter.d.ts.map